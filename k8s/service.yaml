apiVersion: v1
kind: Service

metadata:
  name: devops-assignment-service

spec:

  selector:
    app: devops-assignment

  ports:

    - protocol: TCP
      port: 80
      targetPort: 5000

  type: NodePort